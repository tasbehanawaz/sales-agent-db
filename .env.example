# MSSQL Database Configuration
# For local development: DB_HOST=localhost
# For Token Bazaar server: DB_HOST=replace with your database host
DB_HOST=localhost
DB_PORT=1433
DB_NAME=sales_agent_demo
DB_USER=replace with your database username
DB_PASSWORD=replace with your database password

# Optional: for seeding script
SEED_ENV=development
