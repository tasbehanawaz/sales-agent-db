# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_NAME=sales_agent_demo
DB_USER=sales_agent
DB_PASSWORD=sales_agent_password

# pgAdmin Configuration (http://localhost:5050)
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin

# Optional: for seeding script
SEED_ENV=development
